type Event {
	id: ID!
	userId: ID!
	createdAt: AWSDateTime
	payload: AWSJSON!
}

type Mutation {
  createEvent(userId: ID!, payLoad: AWSJSON!): Event
}

type Query { 
	getEvent(id: ID!): Event
}

schema {
    query: Query
    mutation: Mutation
}